name: Briko CI Smoke
on:
  push:
    branches: ['**']     # run on pushes to any branch
  pull_request:
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node -v || true
      - run: echo "Briko smoke test ✅ ref=$GITHUB_REF sha=$GITHUB_SHA"
